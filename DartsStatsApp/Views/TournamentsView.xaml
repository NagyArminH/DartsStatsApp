<?xml version="1.0" encoding="utf-8" ?>
<ContentPage 
             x:Name="PageRoot"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="DartsStatsApp.Views.TournamentsView"
             Title="Tournaments">
    
    <CollectionView ItemsSource="{Binding GroupedTournaments}">
        <CollectionView.ItemsLayout>
            <LinearItemsLayout Orientation="Vertical" ItemSpacing="5"/>
        </CollectionView.ItemsLayout>
        
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <toolkit:Expander IsExpanded="False">

                    <toolkit:Expander.Header>
                        <Border Stroke="#E2E8F0" StrokeThickness="1" StrokeShape="RoundRectangle 10" BackgroundColor="#F8FAFC" Padding="10" Margin="10,2">
                        <Grid Padding="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="30"/>
                            </Grid.ColumnDefinitions>
                            
                        <Label Text="{Binding MonthName}"
                           FontAttributes="Bold"
                           FontSize="18"
                           Padding="5"/>

                            <Label FontSize="18" Grid.Column="1" Text="▼">
                            <Label.Triggers>
                                    <DataTrigger TargetType="Label" Binding="{Binding Source={RelativeSource AncestorType={x:Type toolkit:Expander}}, Path=IsExpanded}" Value="True">
                                        <Setter Property="Text" Value="▲"/>
                                    </DataTrigger>
                            </Label.Triggers>
                        </Label>
                        </Grid>
                        </Border>
                    </toolkit:Expander.Header>



                    <CollectionView ItemsSource="{Binding Tournaments}"
                                    SelectionMode="None"
                                    Margin="5,10,0,10">
                        <CollectionView.ItemsLayout>
                            <LinearItemsLayout Orientation="Vertical" ItemSpacing="7"/>
                        </CollectionView.ItemsLayout>
                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <Border Stroke="#E2E8F0" StrokeThickness="1" StrokeShape="RoundRectangle 10" BackgroundColor="White" Padding="5">
                                <Grid Padding="5"
                                      HorizontalOptions="Fill"
                                      WidthRequest="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=Width}"
                                      ColumnDefinitions="*,Auto">
                                    
                                <Grid.GestureRecognizers>
                                        <TapGestureRecognizer 
                                            Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.NavigateToTournamentDetailsView}"
                                            CommandParameter="{Binding .}"/>
                                </Grid.GestureRecognizers>
                                    
                                    <Label Text="{Binding TournamentName}"
                                           Grid.Column="0"
                                           VerticalOptions="Center"
                                           FontSize="13" 
                                           FontAttributes="Bold"/>
                                    
                                    <Label Text="{Binding StartDate, StringFormat='{0:yyyy.MM.dd.}'}"
                                           Grid.Column="1"
                                           VerticalOptions="Center"
                                           HorizontalTextAlignment="End" 
                                           FontSize="13"
                                           FontAttributes="Bold"/>
                                </Grid>
                                </Border>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </toolkit:Expander>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>